# 数字人工作流：仓库约定

- 这是通用可分享代码，先读 README、INSTALL_FOR_CODEX 与 Skill。私人工作区不属于代码仓库。
- 不写入个人路径、账号/voice/avatar ID、密钥、原始录音、私人文案、云端回执或成片。示例使用虚构文案与无身份空配置。
- 原稿、长期配置、job 状态、时间轴分别有单一职责；不要根据聊天内容重复付费请求。更新必须保留已成功任务与素材。
- `uncertain` 表示可能已收费，不自动重试；不得通过删状态、换 ID、切换账户绕过。
- 新供应商/地区/计费路径或提高预算需要用户决定。已有同范围授权不重复确认。安装不等于生成授权。
- 真正有音频依据的时间戳才能进入字幕；不能用均分时间、猜测发音、空视频替代正式产物。
- 新增媒体来源要记录权利；不要去除平台水印。发布代码不等于发布用户素材。
- 先复用并核实官方接口与固定依赖的许可，再改对应职责层。不得把单元测试通过当成云服务全链路已测。
- 改代码用 `.venv/bin/python -m unittest discover -s tests -v`；涉及合成需实际运行本地渲染、查看帧和全解码。代码与检查/渲染哈希必须相符。
- `scripts/audit_release.py` 检查 Git 暂存集合；推送前还需人工核对全部暂存文件、来源和二进制白名单。
- 新人使用过程中由 Codex 编写分镜和验收文件；尊重工具自身调用/等待限制，不让新人去操作内部状态文件。

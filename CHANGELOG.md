# 更新记录

## 0.5.0 — 2026-09-21

按用户要求移除剪映专用自动化相关内容：下线原生草稿导出与重定位入口，移出对应第三方代码、包数据和许可证附件，清除相关参考仓库链接及使用教程。保留当前独立工作流使用的通用剪辑方法与代码。

- MiniMax、HeyGen、人物校准、字幕对齐、语义插片、圆形画中画、混音与 Hyperframes 直接导出保持可用。
- 新人不再被引导安装或操作桌面剪辑软件；素材与可编辑 HTML 工程仍可交付，ZIP 按需。
- 不触碰用户旧工程和已交付媒体，不重放付费生成，不改写 Git 历史。旧版本扩展文件留在原工作区，新包不自动附带。
- 历史说明中的专用集成教程已移除；下面仅保留对当前能力仍有意义的记录。测试范围见 VALIDATION.md。

## 0.4.0 — 2026-09-21

将长片制作和用户验收中的新经验转成可复用的 Codex 监制流程，降低选材、人物连续出镜与导出时的重复工作。

- 新增监制执行规范：最少用户输入、要求逐项映射、通用身份/外观约束、按实际账户选择模型与原生画质。合格内容复用，人物表现与声音优先。
- 新增真实插片检索手册：Pexels 站内/API 的动作级检索、竖屏构图筛选、逐段看动效、源文件/裁切/许可台账；电影索引只用于定位，不把公开视频当作已获商用许可。没有新增收费素材或视频模型适配器。
- 新增纯本地人物覆盖规划：只生成所需区间，或连续圆形画中画；按音频 sample 映射原时轴与生成 reel，排除静音/其他说话者，避免静帧张嘴和不相关口型。
- 修正竖屏源的 circle 蒙版为真圆；保留独立头像裁切、人物安全区、字幕与图解错位布局规范。字幕/效果留在后期。
- 增加一次性本地导出器与 macOS LaunchAgent、超时进程树停止、防重复/同名覆盖、工程哈希/全解码/登记恢复；不使用自动保活，不把编码完成当作视听通过。
- 固定 Hyperframes 0.8.48 的运行时适配：macOS 竖屏捕获保护、短入场截止时间精确采样；不修改依赖文件或移除审计。Node 最低 22.15，新增 psutil 7.2.2。
- 完善字幕可读性、角色声音、眨眼与周期动作排查、独立后期分工、必要素材保留及版本化恢复。默认目录交付，ZIP 按需创建。

无旧功能移除、无私人素材/账号发布、无云端付费生成。本版新增范围和实际证据见 [VALIDATION](docs/VALIDATION.md)；云端自然度仍需当前用户素材的短片验收。多 reel 与定制镜头由 Codex 按 Skill 编排，并非基础 `run` 命令自动完成。

## 0.3.3 — 2026-09-21

解决“用户指定的模型没有落实到实际付费请求”和“已被否定的短样片仍可能成为长片基线”的问题。

- 新任务可绑定 brief.model_requirements；MiniMax 与 HeyGen API/MCP 检查配置及实际参数，不匹配即阻止付费提交，不能修改 brief 绕过。
- 新增 revoke-baseline：保留认可记录与源素材，停止新任务复用；已经提交的付费任务仍可恢复。
- Skill 增加 look 图片/视频外观对照、周期动作与口型连续检查、角色音色选择、分层工具审计与有限候选预算。图片区别于视频生成，逐句字幕区别于动态强调；已有合格素材可复用。
- 沿用 v0.3.2 的直接导出默认规则。没有移除功能、强制模型/套餐、修改历史付费状态或加入个人素材。

代码校验不能证明真人感。Avatar V 具体身份、连续动作和新声音仍须按真实样片验收；本版本未宣称已完成新的云端长片验收。

## 0.3.0 — 2026-09-20

把全数字人生成与后期剪辑拆开：HeyGen 负责干净口播，字幕、插片、镜头和混音在独立后期处理。

- 新增 edit-build / edit-render 与不可覆盖修订，复用父任务数字人和配音，旧收费状态不变。
- 统一时间轴支持句段剪切、重排、同步变速、原生文字字幕/标题、视频与图片插片、独立音乐/音效和线性镜头关键帧；剪口穿过发音区间时拒绝。
- Hyperframes 可独立生成成片，复杂 HTML 可另行封存。
- 新增本地视频/图片/声音素材登记、哈希检查、来源/权利记录。
- Skill 增加剪辑工具选择、全数字人约束、语义插片提示词和原生编辑验收；不把真人实拍替代成片路径作为默认选项。

保留现有 API、会员 MCP、导入数字人、克隆、真人感门槛、旧 storyboard、HTML 动画和交付能力。没有数据迁移、账单切换或付费生成。主观人物自然度与新生成视频效果仍需实际样片验收，见 VALIDATION。

## 0.2.1 — 2026-09-20

修复跨系统验证发现的 Windows 旧控制台编码问题：安装器与 CLI 的 JSON 输出使用可跨编码解析的 Unicode 转义，复制安装定位文件明确按 UTF-8 读取。补充非 UTF-8 控制台和中文路径回归；不改变模型参数、授权或媒体结果。v0.2.0 标签保留历史，推荐安装 v0.2.1 或之后版本。

## 0.2.0 — 2026-09-20

解决原流程从配音直接进入视频、静态形象与动作参考缺少前置检查的问题，把真人感验收提前到正式长片之前。

- 新增原素材本地检查、静态 look/姿势/机位/引擎能力质量计划；API 与 MCP 都在上传和创建之前校验。
- 新增最终配音 SHA256 绑定的试听记录。口播未听过、音频变化或校准超长时不自动提交口型。
- 新增用户认可的短样片基线，匹配的声像/动作配置可跨文案复用；主要设置改变时重新验证。首次条件由 Codex 编排，不要求用户手写 JSON。
- API/MCP 共用参数构造；支持 Avatar V 的同组动作参考与简短可选动作提示词。MiniMax 支持可选情绪及模型兼容检查，默认省略，不更换用户选定的声音/模型。
- 新增场景、口语表达、动作与本地剪辑四层提示词；记录少量变量实验和跨任务累计费用，反对无限抽样。原生清晰度、技术通过与真人感不混为一谈。
- 新任务交付增加动态片段、场景和音轨证据，静态截图不再足以代表动态验收。已有交付仍可恢复。
- 复制安装的 Skill 支持 `--update`，更新前保留完整本地备份；符号链接安装随仓库更新。

兼容：保留 API / 会员 MCP / 本地导入与现有字幕、动画、打包。旧配置新增字段可省略；不重写旧任务快照。已提交或结果不确定的旧请求只恢复/核对，不重新创建。

没有新增依赖、硬编码套餐价格、发布私人素材或为此次升级执行收费生成。测试范围与剩余条件见 [VALIDATION](docs/VALIDATION.md)。

## 0.1.0 — 2026-09-19

首个独立通用版本：安装、用户配置、声音克隆/配音、HeyGen API/MCP 桥接、断点恢复、字幕、Hyperframes 合成和素材包。

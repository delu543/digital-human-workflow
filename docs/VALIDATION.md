# v0.2.0 验证范围

核验日期：2026-09-20。本轮只做代码、Skill、协议核对和本地合成测试，没有为测试生成新的云端真人媒体。

| 检查 | 实际结果与范围 |
|---|---|
| 回归测试 | 57项通过：包含上传前质量门槛、MCP不绕过验收、当前声音哈希/试听方式、姿势与引擎不匹配、校准时长、过期能力、撤销授权、旧任务恢复、基线复用/变更失效、API/MCP参数、情绪兼容、复制Skill备份升级与异名安装保护 |
| 官方 Skill 校验 | quick_validate.py 通过；使用项目虚拟环境执行，系统 Python 缺少 PyYAML，不修改系统环境 |
| 新用户空目录 | init/doctor 正确列出未配置项；无作者密钥、声像或预算继承 |
| 本地渲染回归 | 12.0秒合成画面/测试音，1080×1920、25fps；Hyperframes check、全文件解码、时长/音轨/工程一致性通过；查看静态结果，不作真人验收 |
| 原素材检查命令 | 在该合成成片实际运行，读取 H.264/AAC 和 BT.709 元数据，提取3帧与3个连续片段；证明工具可运行，不证明能自动识别暗眼部或手部异常 |
| 公开内容边界 | 通用代码/示例/测试/官方来源文档；私人质量计划、基线、原片、克隆声音、任务回执均留在用户工作区 |

新增测试模拟云端，不能证明所有账户能调用 Avatar V，也不能证明新提示词已经改善某个用户的真实视频。本版**未做新的付费视听样片验收**；这部分须在用户提供足够素材及实际授权后验证。接口、额度、实时价格和主观真人感仍有外部条件。

新流程将原片检查、配音试听、短片基线与最终动态检查交给 Codex；程序检查记录和状态，无法自动证明检查者确实听过、看过。跨任务/静态 look 费用仍由 Codex 按私人实验账本核对，CLI仅强制本任务预算和一次性请求。

## v0.1.0 保留的历史证据

记录日期：2026-09-19。公开报告只含通用测试结论；私人声像、文案、账号 ID 和运行日志不随仓库发布。

| 检查 | 实际结果 | 能证明什么 |
|---|---|---|
| 独立本地安装 | macOS arm64，Python 3.12.14，Node 24.15.0，bootstrap 成功 | 新代码目录的 Python/Node/编码器可运行，不依赖原项目的虚拟环境 |
| 本地转写安装 | 校验源码归档与模型哈希，独立编译 whisper.cpp 1.9.4，运行 base DTW 成功 | 编译和声学 token 获取可用；模型文件复用已核验缓存 |
| 单元/回归测试 | 36项通过 | 任务锁、输入不可变、预算前置、超时防重复、回执恢复、路由约束、字幕时间/编码、交付完整性 |
| Skill | 官方 quick_validate 通过，安装脚本成功，同名安装不会覆盖 | 格式与本地发现路径符合要求；并非所有模型行为都被形式化验证 |
| 新人空工作区 | init/doctor 成功，准确列出缺失账号/声像/授权 | 不继承作者配置，也不把未配置当已就绪 |
| 真实素材本地回归 | 已获授权的18.562秒源视频，输出18.6秒、720×1280、25fps，有音轨；全解码成功 | 对已有本人声像能完成字幕、标题/步骤动画、技术检查与打包 |
| 声音保真/同步 | 开头、中间、结尾与已认可源音频的相关系数均大于0.9998，最佳偏移0秒；查看6张成片帧 | 本地包装没有改变音色或引入可测的音轨偏移；不替代完整主观口型试听 |
| 完整素材包 | 23个文件，约44MB，ZIP CRC通过；无密钥、个人配置或云端回执 | 白名单打包能交付视频、字幕、声像、工程、来源与验收材料 |
| 五分钟压力测试 | 300.0秒、1080×1920、25fps，MP4约10.6MB；Hyperframes check、全解码与尺寸/音轨/时长检查通过 | 本地长视频字幕、标题、步骤、柱图与插图可渲染；使用合成画面/测试音，不是五分钟真人云端样片 |
| 隐私发布检查 | 暂存文件扫描；私人原稿和配置内的身份标识零匹配；二进制仅许可字体白名单 | 已检查的发布集合未包含作者私人数据；不能代替未来每次提交的审查 |
| 原工程保护 | 原私有工程18个基线文件哈希未变化 | 新产品独立建立，没有覆盖原脚本、配置或模板 |

## 真实发现与处理

- 预算不足时最初会先上传音频，已改为上传前检查预算，并有测试验证上传次数为零。
- 渲染默认尝试下载专用浏览器；已让启动器优先复用本机可用 Chrome。没有浏览器时仍需要首次下载，网络与磁盘需求不能省略。
- 新编译的 base 转写曾把短样片中的4个中文字识别错，原稿一致性检查正确阻止出片。经已认可源音频与原始声学转写核对词语后，保留新转写的实际时间戳，修正并生成11条字幕成功。**不能声称 base 模型对任意中文稿零人工校对。** Codex 能确认的内容自行核对，不能确认时请用户处理该歧义。
- whisper.cpp 的 token JSON 可能包含截断的 UTF-8 标点字节。解析允许替换无效字节；替换字符不算台词，丢失真实文字仍触发原稿不一致。测试覆盖两种情况，不自动猜字。
- 检查、工程与渲染通过哈希绑定，修改配图/CSS等次级文件也会使旧检查失效；不能把旧验收沿用到新工程。

## 尚未由本次发布验证

未为发布测试新增付费调用；通用版本的 MiniMax/HeyGen API 使用官方协议适配与模拟恢复测试，未在每个用户账户执行真实收费全链路。此前个人短样片得到的效果不代表任何新账户都拥有相同权限。

未实测五分钟付费云端真人生成、任意音色/头像质量、所有套餐、所有地区、原生 Windows 的完整媒体安装。GitHub Actions 会在 Ubuntu/macOS/Windows 跑无密钥单测；完整渲染平台范围以实际报告为准。第三方接口改变、账号余额/权限、语音识别错误及新素材的视觉布局仍需检查。

## 可复现命令

```sh
.venv/bin/python -m unittest discover -s tests -v
.venv/bin/python scripts/smoke_render.py --seconds 12
# 可选长测试，无云端调用，可能需要数分钟至更久：
.venv/bin/python scripts/smoke_render.py --seconds 300
```

合成测试的字幕是明确标记的测试时间轴，不用于声学对齐验证，也不会被程序记录为本人验收通过。测试输出在被 Git 忽略的 `.runtime/`。平台实际使用仍需首次授权样片验收。
